# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.3.0] - 2026-08-13

### Changed

- 重做技能市场与手动安装的核心路径：安装分为只读准备和确认提交两阶段，写入前展示规范检查、安全评分、风险明细、文件清单、精确目标和启用平台；高风险内容需要额外确认。
- 市场安装改为精确选择：点击哪个 skill 只安装哪个，多 skill 仓库中的其余内容会明确列为未安装。
- 安装来源在临时目录中冻结，确认后原子写入本体库并验证软链；冲突在写入前一次检查，失败可用同一份已审查内容重试，取消或成功后自动清理。
- lock 记录同时保存用户请求的 tag/ref 与实际解析出的 commit SHA，便于复现和审计。
- 移除依赖本机 Claude CLI 的 AI 分析入口，聚焦 Skill 管理的核心链路。
- 全面更新应用图标与发布元数据；正式构建版本统一由 `VERSION` 文件管理。
- Bundle ID 调整为项目专属标识，并在首次启动时自动迁移旧版偏好。

### Added

- 新增 MIT 许可证、贡献指南、安全政策、隐私说明和 GitHub Actions 发布门禁。
- 新增 Apple Silicon + Intel 通用构建，以及 Developer ID 签名、公证、校验文件生成脚本。

### Fixed

- 修复 GitHub `tree/<branch>/<subdir>` 安装忽略分支、可能读取默认分支错误内容的问题。
- Git 安装和版本解析增加 120 秒超时，弱网或远端异常时不再无限等待。

## [1.2.0] - 2026-08-07

### Changed

- 更新应用图标，并支持在侧栏隐藏未创建目录的空平台。

## [1.1.1] - 2026-08-07

### Fixed

- 修复启动阶段安全扫描阻塞首屏数据，长时间显示 0 个 skill 的问题。

## [1.1.0] - 2026-08-06

### Added

- 新增逐 skill 指纹缓存；只有发生变化的 skill 才重新解析和扫描，提高启动速度。

## [1.0.0] - 2026-08-05

首次完整功能发布：本体库 + 软链的统一管理底座之上，补齐场景、市场、清单、安全扫描等全套能力。

### Added

- 统一视图：扫描本体库（`~/.agents/skills`）与各 Agent 平台（Qoder / Claude Code / Codex / Cursor / iFlow 等）的 skills 目录，按本体去重并展示每个 skill 在各平台的存在形式（真实目录 / 软链接 / 断链）。
- 场景（Preset）：命名 skill 组，一键激活 / 停用整组到目标平台（未指定平台时对所有非本体库平台生效）；侧栏「场景」区新建 / 重命名 / 删除，skill 详情「加入场景」；已是目标状态的项自动跳过，持久化到 UserDefaults。
- 技能市场：工具栏「市场」接入 skills.sh——搜索 API（与 `npx skills find` 同源）+ 首页热门榜单解析，显示安装量、按需补取描述，一键安装；多 skill 仓库整仓安装到本体库。
- 技能清单（Manifest）：工具栏「清单」菜单导出 / 导入 JSON 清单（`formatVersion: 1`，含名称、安装来源、启用平台、标签）；导入先 dry-run 生成计划，预览"将安装 / 补启用 / 跳过"计数后执行，单个失败不中断整体流程。
- 一键收编到本体库：管家「同步」「清理」页顶部横幅展示散落 skill 数量，一键把散落本体迁移进本体库并原地补软链；同名冲突自动"链接化"（散落目录进废纸篓可恢复，改建指向本体库已有版本的软链），散落版本更新时跳过并提示手动合并；软链登记到外部本体（如 iCloud）的视为已收纳。
- 安全扫描（体检第 9 项）：表驱动规则覆盖危险删除 / 远程执行 / 凭据访问 / 网络外发（带域名白名单）/ 权限提升五大类，按严重度扣分给出 0–100 评分与「安全 / 注意 / 风险」三档等级；高危 / 中危命中转为体检问题。
- 项目工作区：管家新增「项目」页，注册项目目录（按标准化路径查重）后扫描 `.claude/skills`、`.agents/skills` 等项目级约定位置的 skills，支持与本体库双向复制同步（「收进本体库」/「从本体库导入 skill…」）；同名冲突报错不覆盖。
- 复制（copy）启用模式：`SkillOps.enable` 除相对软链外支持以副本方式启用到平台（skill 详情页「平台状态」区「以副本方式启用到…」菜单），副本写入 `.skillhub-copy` 标记，禁用与移入废纸篓时凭标记区分副本与本体（无标记的真实目录照旧拒绝删除）。
- @版本安装：安装源支持 `仓库地址@tag`（或 commit SHA）固定版本；指定 ref 时完整克隆后 checkout，避免浅历史里找不到 tag。
- 体检 Doctor：检测断链、缺少 frontmatter、缺 description、重名冲突、目录名与 name 不一致、引用文件缺失、体积异常、孤儿本体、本体不在本体库，支持单条修复与一键全部修复。
- 安装：支持 Git 仓库 URL（含 tree 子目录形式）与本地目录，自动发现 SKILL.md 并安装到本体库、按勾选建软链。
- 批量操作：单 skill 同步到所有平台；按平台批量启用 / 禁用。
- 智能搜索：名称 / 描述 / 标签 / 作者加权匹配 + 模糊匹配；关键词自动分类；按作者分组（≥3 个 skill 的作者单独成组，长尾归并进「其他」组，未知作者单独成组）。
- 收藏夹与最近使用：收藏持久化；基于文件修改时间推断使用频率（活跃 / 闲置 / 休眠）。
- 冗余检测与清理建议：同名与标签重叠分组；体积过大、长期未修改、本体不在本体库的清理建议。
- Git skill 更新检测：fetch 对比远端 commit，提示并可一键 pull 更新。
- AI 分析：调用本机 claude CLI 生成 tags 与 summary 并写回 SKILL.md frontmatter。
- 自定义 Agent 平台与自定义扫描目录；自动检测 Claude Code / Codex / Cursor / Cline / Windsurf。
- 工具栏中文标签；列表选中高亮；标签 / 胶囊流式换行布局（FlowLayout / WrappingHStack）。
- 工程化：`Scripts/make_app.sh` 一键打包 SkillHub.app（Info.plist + 资源 bundle + 可选 icns）；`.gitignore`；README；130+ 单元测试覆盖解析、扫描、安装、体检、市场、场景、清单、安全扫描、项目工作区等纯逻辑层。

### Changed

- 智能管家面板重组为「总览 / 来源 / 同步 / 清理」四页，「同步」「清理」页顶部接入一键收编横幅。
- 体检在本体库判定上采用"是否被本体库收纳"口径：本体直接在库内，或库内软链解析后指向同一本体的，均视为已收纳，不再误报。
- 批量同步 / 收编 / 修复 / 导入等长任务改为后台执行、统一收尾（关繁忙态 + 通知 + 刷新），循环结束后只刷新一次。

### Fixed

- 平台管理：自定义 / 自动检测平台按实际目录去重，内置平台优先；启动时清理历史遗留的 `auto_*` 重复平台，不再出现同一目录两个平台。
- 修复模糊搜索（fuzzyMatch）匹配错误。
- 修复 frontmatter 解析与写回的若干边界问题（有 / 无引号的 name 写法、缺失字段补写位置等）。
- 修复路径前缀判断误判（标准化路径 + 目录边界比较，避免 `/foo/bar2` 被当作 `/foo/bar` 的子路径）。
- 修复标签 / 筛选缓存不失效返回陈旧数据的问题：移除失效缓存，改为实时计算。
- 修复后台批量操作的并发竞态与并发捕获警告：跳回 MainActor 时只携带不可变值，检测结果应用提取为独立方法。
- 修复跨平台同步缺失时未批量刷新、状态停留在旧值的问题（批量操作结束后统一 refresh）。
- 修复批量同步性能：场景激活 / 停用、批量启用 / 禁用等循环内不再逐次刷新，结束后统一刷新一次。
- 列表选中高亮修复；死代码清理。

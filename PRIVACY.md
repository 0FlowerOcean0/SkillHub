# 隐私说明

SkillHub 不包含账号系统、广告、遥测或分析 SDK。收藏、场景、自定义平台和项目列表保存在本机 UserDefaults；扫描缓存与安装来源记录保存在 `~/.agents` 下。

只有用户主动使用相应功能时才会产生网络访问：

- 技能市场会访问 `skills.sh`，搜索时会发送搜索关键词，并读取榜单和技能描述；
- 安装或更新会调用系统 `git` 访问用户选择的 Git 仓库；
- 打开技能详情或仓库页面会访问对应网页。

SkillHub 不会把本地 SKILL.md 内容上传到 SkillHub 自有服务器。第三方网站、Git 服务和下载内容适用各自的隐私政策与服务条款。

用户可以通过删除应用偏好和 `~/.agents/.skillhub-scan-cache.json` 清除本地界面设置与扫描缓存；删除 `~/.agents/.skill-lock.json` 会同时失去安装来源记录，请谨慎操作。
